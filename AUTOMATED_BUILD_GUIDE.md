# 🚀 Автоматизированная Сборка с Зависимостями

## 🎯 Решение Проблемы

Этот гайд решает проблему **"Failed to fetch"** в собранном Electron приложении, которая возникает из-за отсутствия зависимостей backend сервера.

## ⚡ Быстрый Старт

### Windows (PowerShell)
```bash
npm run build-with-deps
```

### Linux/macOS (Bash)
```bash
npm run build-with-deps-bash
```

## 🔧 Что Делают Скрипты

1. **Установка зависимостей** основного проекта
2. **Установка зависимостей backend** (включая node_modules)
3. **Сборка frontend** (React + Vite)
4. **Сборка Electron приложения** с включением всех зависимостей

## 📁 Результат

После успешной сборки:
- Готовое приложение: `electron-dist/Telegram Lead Scanner-win32-x64/`
- Все зависимости включены автоматически
- Backend сервер запускается без ошибок
- Нет проблем с "Failed to fetch"

## 🆚 Сравнение Методов

| Метод | Описание | Плюсы | Минусы |
|-------|----------|-------|--------|
| **Ручная установка** | Установка зависимостей после сборки | Быстрое решение | Нужно повторять для каждого пользователя |
| **Исправление конфигурации** | Обновление electron-builder.yml | Средняя сложность | Требует пересборки |
| **Автоматизированная сборка** ⭐ | Скрипты с полной автоматизацией | Полностью автоматизировано, надежно | Немного дольше сборка |

## 🔍 Проверка Результата

После сборки проверьте:

1. **Наличие node_modules в backend:**
   ```
   electron-dist/Telegram Lead Scanner-win32-x64/resources/app/backend/node_modules/
   ```

2. **Запуск приложения:**
   ```bash
   ./electron-dist/Telegram Lead Scanner-win32-x64/Telegram Lead Scanner.exe
   ```

3. **Проверка API:**
   - Откройте приложение
   - Попробуйте подключить Telegram
   - Не должно быть ошибок "Failed to fetch"

## 🐛 Устранение Проблем

### Ошибка выполнения PowerShell
```bash
# Разрешить выполнение скриптов
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Ошибка прав доступа в Linux/macOS
```bash
# Сделать скрипт исполняемым
chmod +x build-with-deps.sh
```

### Большой размер приложения
Это нормально - включение node_modules увеличивает размер, но обеспечивает стабильную работу.

## 📋 Чек-лист Готовности

- [ ] Установлены зависимости основного проекта (`npm install`)
- [ ] Backend работает в режиме разработки
- [ ] Frontend собирается без ошибок (`npm run build`)
- [ ] Запущен автоматизированный скрипт сборки
- [ ] Проверено наличие node_modules в собранном приложении
- [ ] Приложение запускается и работает без ошибок API

## 🎉 Готово!

Теперь у вас есть:
- ✅ Полностью автоматизированный процесс сборки
- ✅ Надежное приложение с включенными зависимостями
- ✅ Решение проблемы "Failed to fetch"
- ✅ Готовое к распространению .exe приложение

---

**💡 Совет:** Используйте автоматизированную сборку для всех будущих релизов, чтобы избежать проблем с зависимостями.